# iOS bootstrap notes

A minimal iOS runner scaffold has been added for source-control completeness.

To generate a full Flutter iOS project structure (recommended), run:

```bash
flutter create --platforms=ios .
```

Then open `ios/Runner.xcworkspace` in Xcode for signing and deployment setup.
